# React B端管理系统

这是一个基于 React 18.3.1 + TypeScript + Vite + Ant Design 的现代化B端管理系统。

## 技术栈

- **前端框架**: React 18.3.1
- **开发语言**: TypeScript 5.8.3
- **构建工具**: Vite 6.3.5
- **UI组件库**: Ant Design 5.26.7
- **样式框架**: Tailwind CSS 3.4.17
- **状态管理**: Zustand 5.0.3
- **图表库**: ECharts 5.5.1
- **路由**: React Router DOM

## 项目结构

```
src/
├── components/          # 公共组件
│   └── Layout/         # 布局组件
├── pages/              # 页面组件
│   ├── Home/           # 首页
│   ├── Login/          # 登录页
│   └── SystemManagement/ # 系统管理
│       ├── UserManagement/      # 用户管理
│       ├── RoleManagement/      # 角色管理
│       └── PermissionManagement/ # 权限管理
├── router/             # 路由配置
├── store/              # 状态管理
├── hooks/              # 自定义Hooks
├── lib/                # 工具库
├── assets/             # 静态资源
└── types/              # 类型定义
```

## 功能特性

### 🔐 用户认证
- 登录/登出功能
- 测试账号：admin / 123456
- 用户状态管理

### 📊 数据展示
- 响应式数据统计卡片
- 系统状态监控
- 最近活动记录

### 👥 用户管理
- 用户列表展示
- 用户增删改查
- 用户状态管理
- 角色分配

### 🛡️ 角色管理
- 角色列表管理
- 权限分配
- 角色状态控制

### 🔑 权限管理
- 树形权限结构
- 权限类型分类（菜单/按钮/API）
- 权限状态管理

## 安装要求

在运行此项目之前，请确保您的系统已安装：

1. **Node.js** (推荐版本 18.x 或更高)
2. **npm** 或 **yarn** 包管理器

### macOS 安装 Node.js

```bash
# 使用 Homebrew 安装（推荐）
brew install node

# 或者从官网下载安装包
# https://nodejs.org/
```

### 验证安装

```bash
node --version
npm --version
```

## 快速开始

1. **安装依赖**
   ```bash
   npm install
   ```

2. **启动开发服务器**
   ```bash
   npm run dev
   ```

3. **构建生产版本**
   ```bash
   npm run build
   ```

4. **预览生产版本**
   ```bash
   npm run preview
   ```

## 开发说明

### 样式规范
- 主内容区域：透明背景，无内边距
- 模块间距：16px marginBottom
- 卡片内边距：使用默认 Card 内边距
- 响应式设计：使用 Ant Design 的 Row/Col 组件

### 表格设计
- 左对齐：用户名、姓名、角色等文本
- 右对齐：数量、金额等数值
- 居中对齐：状态、操作按钮
- 固定列宽：确保表格布局稳定

### 颜色系统
- 主色调：#1890ff (蓝色)
- 成功色：#52c41a (绿色)
- 警告色：#faad14 (橙色)
- 错误色：#f5222d (红色)

## 浏览器支持

- Chrome >= 87
- Firefox >= 78
- Safari >= 14
- Edge >= 88

## 许可证

MIT License